# Handwritten-Digit-Recognition
Handwritten Digit Recognition is an AI/ML project developed using Python, TensorFlow, Keras, OpenCV, and Tkinter. The model is trained on the MNIST dataset to recognize handwritten digits (0–9). Users can draw a digit on a canvas, and the trained neural network predicts the digit accurately in real time.
